services:
  telegram_bot:
    build: .
    container_name: telegram_bot
    env_file:
      - .env
    volumes:
      - bot_data:/bot
    restart: unless-stopped

volumes:
  bot_data:
     driver: local